# Mensagem Simples

""" Enunciado: 
            Armazene uma mensagem em uma variável e, em seguida, exiba essa mensagem. 
            Então altere essa mensagem para uma nova mensagem e mostre essa nova mensagem."""

# Solução:
mensagem = input("Digite sua mensagem: ")
print('{}'.format(mensagem))

mensagem = ('Obrigado por sua mensagem. Até mais!')
print('{}'.format(mensagem))